<Window x:Class="OrganizadorArquivosWPF.Views.UpdatePromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="One Engenharia LTDA – Atualização"
        Width="450" Height="260"
        MinWidth="400" MinHeight="200"
        WindowStartupLocation="CenterScreen"
        Background="#f4f7fa"
        ResizeMode="NoResize">

	<!-- Copia os estilos do MainWindow -->
	<Window.Resources>
		<!-- Cartão -->
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="13"/>
			<Setter Property="Padding"      Value="14"/>
            <Setter Property="Margin"       Value="20"/>
        </Style>

		<!-- Botões arredondados -->
		<ControlTemplate x:Key="RoundedButtonTemplate" TargetType="Button">
			<Border Background="{TemplateBinding Background}"
                    CornerRadius="7"
                    Padding="{TemplateBinding Padding}">
				<ContentPresenter HorizontalAlignment="Center"
                                  VerticalAlignment="Center"/>
			</Border>
		</ControlTemplate>

		<Style x:Key="PrimaryButton" TargetType="Button">
			<Setter Property="Template"       Value="{StaticResource RoundedButtonTemplate}"/>
			<Setter Property="Background"     Value="#007ACC"/>
			<Setter Property="Foreground"     Value="White"/>
			<Setter Property="Padding"        Value="10,6"/>
			<Setter Property="FontWeight"     Value="SemiBold"/>
			<Setter Property="FontSize"       Value="14"/>
			<Setter Property="BorderThickness" Value="0"/>
			<Setter Property="Margin"         Value="0,0,10,0"/>
			<Setter Property="Cursor"         Value="Hand"/>
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="#0585db"/>
					<Setter Property="Effect">
						<Setter.Value>
							<DropShadowEffect Color="#007acc" BlurRadius="13"
                                              ShadowDepth="1" Opacity="0.18"/>
						</Setter.Value>
					</Setter>
				</Trigger>
			</Style.Triggers>
		</Style>

		<Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
			<Setter Property="Background" Value="#F34C4C"/>
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="#ff8383"/>
				</Trigger>
			</Style.Triggers>
		</Style>
	</Window.Resources>

	<Grid>
		<Border Style="{StaticResource CardStyle}" VerticalAlignment="Center" HorizontalAlignment="Center">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="*"/>
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>

				<ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
					<TextBlock x:Name="TxtVersionInfo"
                               Text="Carregando versões..."
                               TextWrapping="Wrap"
                               FontSize="14"/>
				</ScrollViewer>

				<StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
					<Button x:Name="BtnYes" Content="Sim"
                            Style="{StaticResource PrimaryButton}" Width="80"
                            Click="BtnYes_Click"/>
					<Button x:Name="BtnNo"  Content="Não"
                            Style="{StaticResource DangerButton}" Width="80"
                            Click="BtnNo_Click"/>
				</StackPanel>
			</Grid>
		</Border>
	</Grid>
</Window>

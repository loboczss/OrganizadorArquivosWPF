<Window x:Class="OrganizadorArquivosWPF.UpdateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Atualizando Aplicativo..."
        Width="400" Height="150"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        ShowInTaskbar="False"
        Topmost="True"
        Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtStatus"
                   Grid.Row="0"
                   Text="Iniciando atualizacao..."
                   FontSize="14"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <ProgressBar Grid.Row="1"
                     IsIndeterminate="True"
                     Height="20"
                     Margin="0,0,0,10"/>

        <TextBlock Grid.Row="2"
                   Text="Por favor, aguarde. Nao feche o aplicativo."
                   FontSize="12"
                   HorizontalAlignment="Center"
                   Foreground="Gray"/>
    </Grid>
</Window>